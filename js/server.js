const Student = require('./student');
const Teacher = require('./teacher');

const teacher = new Teacher('Alisa', 25, 'Female', 'Run');

const student = new Student('Mila', 16, 'Female', 'Eat');

console.log(teacher);
console.log(student);